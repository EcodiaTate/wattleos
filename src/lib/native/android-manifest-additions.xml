<!-- android/app/src/main/AndroidManifest.xml — ADDITIONS -->
<!--
  ============================================================
  WattleOS - Android Permission Declarations
  ============================================================
  Add these permissions to the existing AndroidManifest.xml
  generated by `npx cap add android`.
  ============================================================
-->

<!-- CAMERA — Observation photo/video capture -->
<uses-permission android:name="android.permission.CAMERA" />
<uses-feature android:name="android.hardware.camera" android:required="false" />

<!-- STORAGE — Media access for gallery picks (Android 12 and below) -->
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

<!-- MEDIA — Photo/video access (Android 13+) -->
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
<uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

<!-- PUSH NOTIFICATIONS -->
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.VIBRATE" />

<!-- NETWORK STATE — Offline detection -->
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

<!-- LOCATION — Excursion tracking -->
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

<!-- SCHEDULE NOTIFICATIONS -->
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

<!-- INTERNET (should already be present) -->
<uses-permission android:name="android.permission.INTERNET" />

<!--
  ============================================================
  Inside <application> tag — Add Firebase for push notifications
  ============================================================

  For FCM push notifications, add google-services.json to
  android/app/ and add the Firebase messaging service:

  <service
    android:name="com.google.firebase.messaging.FirebaseMessagingService"
    android:exported="false">
    <intent-filter>
      <action android:name="com.google.firebase.MESSAGING_EVENT" />
    </intent-filter>
  </service>

  ============================================================
  Deep Links — Intent filter for wattleos.au URLs
  ============================================================
  Add inside the main <activity> tag:

  <intent-filter android:autoVerify="true">
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />
    <data android:scheme="https" android:host="*.wattleos.au" />
  </intent-filter>

  Also create .well-known/assetlinks.json on wattleos.au
  for Android App Links verification.
-->
